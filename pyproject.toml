[project]
name = "otto-deal-hunter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "scrapling[all]>=0.4.14",
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "pydantic>=2.6.0",
    "requests>=2.31.0",
    "playwright>=1.42.0",
    "curl_cffi>=0.7.0",
    "beautifulsoup4>=4.12.0",
]

[tool.vercel]
entrypoint = "backend.main:app"
